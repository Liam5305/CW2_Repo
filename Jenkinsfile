pipeline {
         agent any
         stages {
                 stage('Build Javascript') {
                 steps {
                     
                 }
                 }
                 stage('Push to Docker') {
                 steps {
                    
                 }
                 }
                 stage('Deploy to Kubernates') {
                 
                 }
                 }
              }
}
